import {describe,it,expect} from 'vitest'; import {normalizeJob} from '../src/jobs/normalize-job.js'; import {rejectionReason} from '../src/jobs/filter-job.js'; import {matchJob} from '../src/jobs/match-job.js'; import {applicationId,isDuplicate} from '../src/jobs/deduplicate.js'; import {validPublicRecruitmentEmail} from '../src/email/validate-recipient.js'; import {redactSecret} from '../src/logger.js'; import type {Job,Preferences,Profile} from '../src/types.js';
const prefs:Preferences={roles:['DevOps Engineer'],locations:['India','Remote'],minimumMatchScore:70,maximumApplicationsPerRun:3,maximumJobsToAnalyze:25,rejectSeniorRoles:true,requirePublicRecruitmentEmail:true,requireApproval:true}; const job:Job={id:'1',company:'Acme',title:'DevOps Engineer',location:'Remote India',jobUrl:'https://a.test/jobs/1',description:'Docker Linux GitHub Actions AWS'}; const profile:Profile={name:'N',email:'',phone:'',location:'',skills:['Docker','Linux','AWS'],tools:['GitHub Actions'],education:['B.Tech'],projects:['CI project'],experience:[],certifications:[],text:''};
describe('job safety',()=>{it('normalizes jobs',()=>expect(normalizeJob({...job,title:' DevOps Engineer '} ).title).toBe('DevOps Engineer'));it('rejects senior roles',()=>expect(rejectionReason({...job,title:'Senior DevOps Engineer'},prefs)).toBe('Senior role'));it('validates match score',()=>expect(matchJob(job,profile,prefs).matchScore).toBeGreaterThanOrEqual(70));it('detects duplicates',()=>expect(isDuplicate(job,[{id:applicationId(job),company:'',role:'',resumePath:'',jobUrl:'',matchScore:0,approved:false,sent:false,applicationMethod:'email'}])).toBe(true));it('only accepts professional recipient',()=>{expect(validPublicRecruitmentEmail('jobs@acme.com')).toBe(true);expect(validPublicRecruitmentEmail('me@gmail.com')).toBe(false)});it('redacts secrets',()=>expect(redactSecret('Bearer abcdefghijklmnopqrstuvwxyz')).toContain('[REDACTED]'))});
